extends ../layout.jade
block content
 .container
  h2 All blogs
  -if(blogs)
     - each blog in blogs
       .container
         blockquote
           p 
             a(href="/blog/" + blog.id)= blog.title
           p
             small
              span By 
              a(href="/user/" + blog.author.id)= blog.author.name
              span= " on " + blog.formatedTime()
         p
  -if(blogs.length == 0)
    div(class="alert alert-warning", role="alert")
      p  Sorry, No blogs to show !!